<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>
      <ul>
        <% @topic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.fields_for :skeleton do |g| %>
    <div class="field">
      <%= g.label :question %><br>
      <%= g.text_field :question %>
    </div>

    <div class="field">
      <%= g.label :answer %><br>
      <%= g.text_field :answer %>
    </div>
  <% end %>
<%= f.submit %>
<% end %>
