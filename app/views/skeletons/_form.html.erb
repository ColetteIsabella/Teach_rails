<%= form_for(@skeleton) do |f| %>
  <% if @skeleton.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skeleton.errors.count, "error") %> prohibited this from being saved:</h2>

      <ul>
      <% @skeleton.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.number_field :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
